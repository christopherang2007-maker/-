# Set the real values in Vercel Project Settings → Environment Variables.
# Do not commit a .env.local file with values.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Backward-compatible alternative only:
# SUPABASE_ANON_KEY=
